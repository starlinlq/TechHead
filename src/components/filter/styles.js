import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: { marginTop: "1%", width: "100%" },
  filter:{marginRight: "2%", marginLeft: "2%", border: "solid", borderWidth: 1, borderColor: "lightgrey", padding: "5px", borderRadius: "5px"}
}));
